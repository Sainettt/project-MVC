<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Recipe</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
</head>
<body>
    <nav>
        <a href="/">Home</a>
        <a href="/recipes">Recipes</a>
    </nav>
    <div class="container">
        <h1>Edit Recipe</h1>
        <form action="/recipes/<%= recipeIndex %>/edit" method="POST" class="form">
            <div class="form-group">
                <label for="name">Recipe Name</label>
                <input type="text" id="name" name="name" placeholder="Recipe name" value="<%= recipe.name %>" required>
            </div>
            <div class="form-group">
                <label for="ingredients">Ingredients (comma-separated)</label>
                <input type="text" id="ingredients" name="ingredients" placeholder="Ingredients (comma-separated)" value="<%= recipe.ingredients.join(', ') %>" required>
            </div>
            <div class="form-group">
                <label for="instructions">Instructions</label>
                <textarea id="instructions" name="instructions" placeholder="Instructions" required><%= recipe.instructions %></textarea>
            </div>
            <button class="buttons save-button">Save Changes</button>
        </form>
    </div>
</body>
</html>
